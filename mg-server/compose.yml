services:
  mg:
    build: .
    ports:
      - "8999:8999"
    configs:
      - source: mg_conf
        target: /app/midlegram.yml
    volumes:
      - mgdata:/app/.store


configs:
  mg_conf:
    file: midlegram.yml


volumes:
  mgdata:
